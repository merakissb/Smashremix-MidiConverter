# Dependencias de midi-a-smash.
# Instalar con:  pip install -r requirements.txt
#
# La herramienta trabaja a nivel de mensaje MIDI (leer, remapear instrumentos,
# escribir), para lo cual mido es la biblioteca estándar y mejor mantenida.
# No requiere ffmpeg, ni GPU, ni soundfonts: la conversión es puro MIDI.
mido>=1.3.0
