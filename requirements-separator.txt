# Entorno del SEPARADOR RoFormer (venv-separator/), AISLADO del principal.
# Solo se usa cuando corres con --separador roformer.
#
# audio-separator exige numpy>=2, incompatible con el numpy<2 del entorno
# principal. Por eso va en su propio venv y se invoca por subprocess (CLI).
#
# Crear e instalar:
#   python3 -m venv venv-separator
#   venv-separator/bin/pip install -r requirements-separator.txt

audio-separator
onnxruntime-gpu   # usar 'onnxruntime' (sin -gpu) si no tienes GPU NVIDIA
